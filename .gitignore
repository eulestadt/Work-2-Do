# User data – courses, syllabi, Populi cache (never commit)
data/

# Course config – your course list and Populi URLs (never commit)
config/courses.yaml

# Portal SQLite dev DB (user data)
portal/prisma/*.db

# Local env (API keys, secrets) – never commit
.env
.env.*
!.env.example

# Python
.venv
__pycache__/
*.pyc
.pytest_cache
.coverage
htmlcov

# Node
node_modules/

# Build / output
output/
.next/

# IDE / OS
.DS_Store
.idea/
*.swp
*.swo
**/xcuserdata/

# Sandbox / deps
.sandbox-deps/
